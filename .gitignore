# Personal data — never in the repo (brainstorm: "code open, data private")
*/profile.md
sessions/

# Secrets — keys and credentials live in .env, never here
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
